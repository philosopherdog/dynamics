#import <UIKit/UIKit.h>

/// Main view controller that lists photos from the camera roll. Allows the user to tap on a photo
/// to display the full size version. This controller also supports a custom pinch/zoom/rotate
/// gesture with a custom view controller transition to the detail view.
///
@interface MainViewController : UIViewController

@end
